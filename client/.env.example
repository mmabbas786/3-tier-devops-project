# For local development without Nginx reverse proxy, point to API server (e.g., http://localhost:5000/api)
# In production with Nginx reverse proxy, leave empty or set to /api
REACT_APP_API=http://localhost:5000/api
